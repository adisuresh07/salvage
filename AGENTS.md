# Repository agent instructions

## Agent skills

Keep this section synchronized with the corresponding section in both
`AGENTS.md` and `CLAUDE.md`.

### Issue tracker

Issues are tracked only in the personal GitHub repository owned by
`rajpaladitiya`. Organization repositories, including EC-aware, are out of
scope. See `docs/agents/issue-tracker.md`.

### Domain docs

This repository uses a single-context documentation layout rooted in
`CONTEXT.md`, with architecture decisions under `docs/adr/`. See
`docs/agents/domain.md`.
